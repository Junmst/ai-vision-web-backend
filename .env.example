# 生产环境务必替换为随机长字符串
JWT_SECRET=replace-with-a-random-secret

# 可选：不填写时，用户需通过 /api/ai-configs 配置模型
DEFAULT_AI_BASE_URL=https://your-provider.example/v1
DEFAULT_AI_API_KEY=your-api-key
DEFAULT_AI_MODEL=your-vision-model
